name: Keep Streamlit Awake
on:
  schedule:
    - cron: '0 */6 * * *'  # tiap 6 jam
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: curl -s -o /dev/null -w "%{http_code}" https://dashboard-ggts9v4ymhychvxjczousd.streamlit.app/
